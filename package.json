{
  "name": "@changebot/widgets-monorepo",
  "version": "0.0.1",
  "private": true,
  "description": "Changebot widgets component library monorepo",
  "scripts": {
    "build": "pnpm --filter @changebot/core run build && pnpm --filter @changebot/widgets-react run build && pnpm --filter @changebot/widgets-vue run build",
    "start": "pnpm --filter @changebot/core run start",
    "test": "pnpm --filter @changebot/core run test"
  },
  "devDependencies": {},
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/changebot-ai/widgets.git"
  }
}
